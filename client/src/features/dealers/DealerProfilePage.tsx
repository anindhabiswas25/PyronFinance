import { StubPage } from '../../app/StubPage';

export default function DealerProfilePage() {
  return <StubPage title="Dealer profile" summary="Bond history, quote outcomes and published evidence for one dealer key." />;
}
