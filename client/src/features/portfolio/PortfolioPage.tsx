import { StubPage } from '../../app/StubPage';

export default function PortfolioPage() {
  return <StubPage title="My trades" summary="Your trade history, encrypted and stored only on this device." />;
}
