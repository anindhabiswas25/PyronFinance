import { StubPage } from '../../app/StubPage';

export default function ActivityPage() {
  return <StubPage title="Activity" summary="Resolved quotes, releases, bonds and slashes, read from the chain. No prices." />;
}
