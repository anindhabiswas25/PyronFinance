import { StubPage } from '../../app/StubPage';

export default function ReceiptPage() {
  return <StubPage title="Trade receipt" summary="What the chain shows about this quote, and what only this device knows." />;
}
