import { StubPage } from '../../app/StubPage';

export default function VenuePage() {
  return <StubPage title="Venue" summary="Quotes a dealer can’t take back. Live protocol totals and the most recent events, read from the chain." />;
}
