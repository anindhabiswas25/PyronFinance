import { StubPage } from '../../app/StubPage';

export default function DealPage() {
  return <StubPage title="Become a dealer" summary="Run a Dealer Node or quote from the browser, and size your bond." />;
}
