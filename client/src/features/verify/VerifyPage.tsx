import { StubPage } from '../../app/StubPage';

export default function VerifyPage() {
  return <StubPage title="Verify" summary="Check a signed reveal or open a disclosure note. Nothing leaves the browser." />;
}
