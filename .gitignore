node_modules/
dist/
contracts/managed/*/keys/*.prover
# The web client bundles these into dist/ (vite.config.ts), and hosted builds (Vercel) have no
# Compact compiler to regenerate them, so the protocol contract's prover keys are committed.
!contracts/managed/otc-protocol/keys/*.prover
.env
.env.local
secrets/
*.log
# Operator config: never committed (CLAUDE.md "Do not"). dealer.example.toml is the template.
packages/dealer-node/dealer.toml

.wallet-state/
otc-midnight-db/
otc-midnight-db-*/
