# pnpm re-runs a scoped `install` before any `run`/`exec` invoked from inside a
# workspace package, which resolves that package as its own project root and
# loses the root's `allowBuilds` policy. Disable it: `pnpm install` at the root
# is the single source of truth for this repo.
verify-deps-before-run=false
